# Downloaded papers and extracted text — copyrighted content, never commit
papers/
text/

# Python environments and bytecode
code/.venv/
__pycache__/
*.pyc

# Transient per-run fetch/extract logs (logs/extractions.csv is the audit trail)
logs/*.log

# Derived QUEST-filtered export — regenerate with code/tools_export.py
database/aimdb_no_quest.csv
